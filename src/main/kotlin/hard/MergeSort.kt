package hard

fun mergeSort(array: IntArray): IntArray {
    // TODO: Implement merge sort algorithm
    // 1. Base case: if array has 1 or 0 elements, return it
    // 2. Divide: split array into two halves
    // 3. Conquer: recursively sort both halves
    // 4. Combine: merge the sorted halves
    // Return a new sorted array (don't modify the original)
    return array
}

private fun merge(left: IntArray, right: IntArray): IntArray {
    // TODO: Implement the merge function
    // Merge two sorted arrays into one sorted array
    // This is a helper function for mergeSort
    return intArrayOf()
}