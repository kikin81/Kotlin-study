package hard

fun binarySearch(sortedArray: IntArray, target: Int): Int {
    // TODO: Implement binary search algorithm
    // Return the index of target in sortedArray, or -1 if not found
    // Array is assumed to be sorted in ascending order
    // Use iterative or recursive approach
    // Time complexity should be O(log n)
    return -1
}

fun binarySearchRecursive(sortedArray: IntArray, target: Int, left: Int = 0, right: Int = sortedArray.size - 1): Int {
    // TODO: Implement recursive version of binary search
    // This is an optional alternative implementation
    // Return the index of target, or -1 if not found
    return -1
}